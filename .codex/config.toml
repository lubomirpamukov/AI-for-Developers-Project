[mcp_servers.playwright]
command = "npx"
args = [
  "-y",
  "@playwright/mcp@latest",
  "--browser",
  "chrome",
  "--output-dir",
  ".playwright-mcp",
  "--save-session"
]
startup_timeout_sec = 30
tool_timeout_sec = 90
default_tools_approval_mode = "prompt"
enabled = true
